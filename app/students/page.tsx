import Link from 'next/link'
import { ActionBand, PageIntro, SiteShell, StatStrip } from '@/components/site-shell'
import { studentProfiles } from '@/lib/student-profiles'
export default function StudentsPage() { return <SiteShell><PageIntro eyebrow="ADOPT-A-STUDENT PROGRAMME" title="Adopt a Student" accent="Programme." text="Meet determined students whose education is one contribution away from continuing. Your KSh 10 is a beginning." /><StatStrip items={[{value:'522533',label:'M-Pesa Paybill'}, {value:'KSh 10',label:'smallest contribution'}, {value:'100%',label:'purpose-led giving'}, {value:'2',label:'student profiles'}]} /><section className="section students"><div className="container"><div className="student-grid">{studentProfiles.map(student => <Link href={`/students/${student.slug}`} className="student-card student-card-link" key={student.slug}><div className="student-image"><img src={student.image} alt={`${student.name} student profile`} /><span>{student.tag}</span></div><div className="student-body"><span className="label">{student.name.toUpperCase()}</span><h3>{student.school}</h3><p>{student.short}</p><div className="student-details"><span>PAYBILL <b>{student.paybill}</b></span><span>ACCOUNT <b>{student.account}</b></span></div><div className="need"><b>{student.need}</b><span>needed to continue</span></div><span className="card-link">View full profile <span>→</span></span></div></Link>)}</div></div></section><ActionBand title="No bright mind left behind." text="Choose a student, give what you can, and become part of their next chapter." /></SiteShell> }
